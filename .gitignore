# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code local state (never commit)
.claude/settings.local.json

# no vendored wheel — the plugin pulls workflow-studio from PyPI (uvx workflow-studio mcp)
workflow-studio/vendor/
